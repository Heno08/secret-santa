<h1>Add all the participants:</h1>

<%= form_for(@event) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :emial %>
  <%= f.email_field :email %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
